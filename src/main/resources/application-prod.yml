spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: 'jdbc:mysql://rm-bp14kjh9te20c7in93o.mysql.rds.aliyuncs.com/user_center?useSSL=false'
    username: hlllg
    password: HGL2020mm
  # session 失效时间
  session:
    timeout: 86400
    store-type: redis
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  #redis 配置
  redis:
    port: 6379
    host: localhost
    database: 1
server:
  address: 0.0.0.0
